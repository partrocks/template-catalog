preset:
  provider: aws
  id: secure-edge
  label: Secure Edge (CloudFront + S3)
  description: Production static hosting with CloudFront in front of S3; use with a PartRocks shareable CloudFront gateway for custom domains and TLS.
  engine: opentofu
  source: opentofu.tf
  capabilities:
    - envConfig
  constraints: {}
  outputs:
    - key: public_url
      value: "{{ FRONT_DOOR_URL }}"
    - key: dns_name
      value: "{{ FRONT_DOOR_DNS_NAME }}"
    - key: canonical_hosted_zone_id
      value: "{{ FRONT_DOOR_HOSTED_ZONE_ID }}"
    - key: CLOUDFRONT_DISTRIBUTION_ID
      value: "{{ CLOUDFRONT_DISTRIBUTION_ID }}"
  slices:
    - instanceKey: origin
      sliceKind: object_storage
      sliceFlavor: s3_static_website
    - instanceKey: edge
      sliceKind: cdn
      sliceFlavor: cloudfront_s3_origin
      refs:
        - relation: origin_bucket
          targetInstanceKey: origin
  preflights:
    artifacts:
      type: archive
      archive:
        path: .
  teardown:
    policy: cleanup
  ui:
    badges:
      - edge-tls
      - production-ready
    securityPosture: edge-tls-only
    costTier: medium
    complexity: medium
    notes: Default production preset. OpenTofu creates S3 + CloudFront; dns_name targets the distribution. Pair with a shareable gateway that has gatewayFlavor cloudfront.
    images:
      - infra/aws/secure-edge/diagram.svg
