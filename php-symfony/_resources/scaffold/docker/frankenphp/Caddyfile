{
	frankenphp
}

:8080 {
	# Served by Caddy only — platform probes must not depend on PHP, DB, or Symfony security/firewall.
	handle /health* {
		respond "OK" 200
	}
	root * public
	encode zstd br gzip
	php_server
}
