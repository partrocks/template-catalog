steps:
  - step: create-project
    label: Creating project
    run:
      - command: "mkdir -p {{projectName}}"

  - step: create-readme
    label: Creating README.md
    copy:
      from: _resources/README.md
      to: "{{projectName}}/README.md"

    
